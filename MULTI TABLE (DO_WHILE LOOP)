#include <stdio.h>
int main() {
  int a,b,c;
  printf("Enter an integer: ");
  scanf("%d", &a);
  b=1;
  do
  { 
      c=a*b;
      printf("\n%d * %d = %d",a,b,c);
      ++b;
  }while(b<=10);
    
  return 0;
}
